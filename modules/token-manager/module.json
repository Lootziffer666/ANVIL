{
  "name": "Token Manager",
  "purpose": "Legacy UI shim: disabled localStorage token storage; use Bellows CredentialVault for secrets.",
  "inputs": [
    "config/json"
  ],
  "outputs": [
    "config/json"
  ],
  "requiredPermissions": [
    "storage.local"
  ],
  "canRunOffline": true,
  "canExport": false,
  "failureBehavior": "Secret creation/retrieval/rotation is rejected; legacy localStorage keys are purged and only disabled metadata remains.",
  "status": "disabled"
}
