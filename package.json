{
  "name": "anvil-shell",
  "version": "0.1.0",
  "private": true,
  "description": "ANVIL web shell packaging helpers for Pake desktop builds.",
  "scripts": {
    "pake:check": "bash scripts/check-pake.sh",
    "pake:build": "bash scripts/build-pake.sh"
  },
  "devDependencies": {
    "pake-cli": "3.14.0"
  }
}
