{
  "name": "Knight",
  "purpose": "File I/O + Diff layer for Anvil",
  "inputs": ["text/plain", "application/json", "text/markdown"],
  "outputs": ["text/plain", "text/diff"],
  "requiredPermissions": ["filesystem.read", "filesystem.write"],
  "canRunOffline": true,
  "canExport": true,
  "failureBehavior": "KnightScopeViolation on out-of-scope paths (QualityState.FAILED). QualityState.DEGRADED on I/O errors. Never silently discards exceptions."
}
